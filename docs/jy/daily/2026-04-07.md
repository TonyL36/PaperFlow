# 2026-04-07 开发记录（PaperFlow）

## 今日目标

- 完成评论交互体验收敛（借鉴 kkbbs）
- 完成登录自动续期链路，减少闲置后反复登录
- 完成云端同步发布，确保部署串行执行

## 今日完成

- 评论区体验改造：
  - 点赞按钮改为图标化样式，移除“复制链接/举报”按钮
  - 评论用户名展示优先使用昵称，不再默认显示用户 ID
  - 用户卡片改为点击触发，且按 `commentId` 单点弹出，避免同用户多条评论同时弹窗
- 评论消息中心优化：
  - 被回复消息展示触发者昵称（优先 displayName）
- 前端请求与页面稳定性：
  - 调整 `useAsyncData` 竞态处理，避免请求切换造成的中断噪声
- 登录保持能力补齐：
  - 前端接入 `POST /api/v1/auth/refresh`
  - 启动时过期自动 refresh、401 自动 refresh+重放、定时与页面回前台 refresh
  - access token 默认 TTL 从 900 秒提升到 14400 秒（4 小时）
- 文档更新：
  - 技术文档更新评论能力现状（5 层、可见性规则、审核与通知联动）
  - 技术文档更新认证链路现状（前端自动续期已接入、默认 TTL 更新）
- 云端发布：
  - 按串行方式执行部署（`COMPOSE_PARALLEL_LIMIT=1`）
  - 处理中途网关 jar 异常并完成修复重启
  - 最终验证前端与 API 可用

## 验证结果

- 前端：`npm run typecheck` 通过
- 后端：`user-service` + `api-gateway` 编译通过
- 云端：
  - `http://47.109.193.180:9628/paperflow/` 返回 200
  - `http://47.109.193.180:9628/api/v1/posts?page[number]=1&page[size]=1` 返回 200
  - 网关健康检查 `{"status":"UP"}`

## 遗留与下一步

- 在生产环境补充 refresh 失败指标（失败率、来源、重试次数）
- 继续收敛评论区交互细节（昵称缓存策略、卡片关闭手势与移动端体验）
