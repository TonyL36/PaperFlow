# 2026-04-05 开发记录（PaperFlow）

## 今日目标

- 修复 topic 日更链路中的 PDF 指向错误与标题错配
- 对齐 topic 与 medical 的 Markdown 生成规范
- 修复帖子详情页与 PDF 阅读页的真实数据映射
- 稳定云端脚本，避免重复与空抓取导致的异常中断

## 今日完成

- topic 生成链路修复：
  - `prepare-topic-papers-review.ps1` 增强 arXiv 抓取容错、Markdown 规范化、标题后缀清理
  - `run-topic-daily.ps1` 与 `upload-reviewed-papers.ps1` 对齐 source 前缀统计与输出
- 前端显示修复：
  - 帖子详情“打开 PDF”改为使用真实 `post.title`
  - PDF 阅读页改为优先使用真实帖子标题与真实 PDF 链接
  - 移除按 postId 映射 demo 论文标题导致的串链
- 存量帖子定点修复：
  - 对异常 topic 帖子执行重写覆盖
  - 校验标题、正文结构、PDF 可达性后再收敛
- 云端发布：
  - 同步更新脚本到 `/opt/paperflow/scripts/`
  - 增量替换前端产物并重启 frontend 容器

## 当日关键提交

- `daeb6f2` fix: widen topic arxiv queries for cybersecurity and bigdata
- `590587a` fix: stabilize topic daily ingestion and output paths
- `773ed18` fix: align topic summary markdown with medical pipeline
- `d2cff4a` refactor: rewrite topic prepare script in medical-compatible style
- `f5455c4` chore: align topic summary prompt with medical markdown structure
- `d8e3228` refactor: restore topic prepare flow to clean pre-fallback style
- `cbda573` fix: use post title for paper links and reader context
- `107325a` fix: resolve paper link/pdf from post data instead of mock mapping
- `5116f4e` fix: strip daily suffix from topic auto titles
- `ac26ab3` fix: append formats block to topic summaries for pdf resolution
- `7978351` fix: keep topic summary content clean without duplicate formats block
- `5f6ddf8` fix: enforce markdown heading line breaks for topic summaries
- `28fa833` fix: harden topic markdown normalization and empty-fetch handling

## 结果与状态

- 主题来源在线重复计数为 0（cybersecurity / bigdata）
- 前端 PDF 跳转与标题映射恢复为真实帖子数据路径
- topic 自动脚本支持“空抓取安全退出”，不再因为无新稿中断后续链路
