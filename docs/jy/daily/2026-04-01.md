# 2026-04-01 开发记录（PaperFlow）

## 今日目标

- 修复论文详情页 Markdown 渲染异常（`##`、`**` 原样显示）
- 修复线上文章中文乱码（`???`）与上传编码一致性问题
- 完成“本地 AI 重写 + 审核 + 重传”闭环
- 排查并恢复前台偶发 `HTTP 502 / SYS_HTTP_ERROR`

## 今日完成

### 1) 论文资源持久化与元数据落库

- 在 content-service 增加论文资源资产表与实体仓储
- PDF 代理改造为“本地持久文件 + 数据库元数据”命中优先
- 生产 compose 增加 PDF 持久卷，容器重启后缓存不丢失

### 2) 审核链路落地（先审后发）

- 新增审核准备脚本，生成待审核 `md/json` 双产物
- 新增上传脚本，仅上传 `APPROVED` 条目
- 新增重写回灌脚本：读取本地 GLM 配置，对已上线文章按原 `postId` 覆盖更新

### 3) 内容质量与编码修复

- 修正审核文档的中文乱码（mojibake）问题
- 上传与回灌脚本改为 UTF-8 字节提交，并显式 `application/json; charset=utf-8`
- 对已上线 6 篇文章执行重写回灌，消除 `One-line conclusion` 中 `???`

### 4) 前端渲染修复

- `RichText` 增加 inline 解析：
  - 支持 `**bold**`
  - 支持 `` `code` ``
  - 支持有序列表 `1. ...`
- 保留块级结构解析，修复详情页正文展示质量

### 5) 线上 502 根因定位与恢复

- 现象：前台偶发 `HTTP 502 / SYS_HTTP_ERROR`
- 定位：frontend Nginx 上游缓存到旧的 `api-gateway` 容器 IP，导致连接拒绝
- 应急恢复：重启 frontend 容器后恢复 200
- 永久修复：Nginx 增加 Docker DNS 动态解析（`resolver 127.0.0.11`）

## 今日关键产出

- 日报：`docs/jy/daily/2026-04-01.md`
- 板块文档：
  - `docs/jy/by-feature/30-cloud-deploy-guardrails.md`
  - `docs/jy/by-feature/31-agent-batch-ingest-and-quality.md`
  - `docs/jy/by-feature/00-index.md`

## 验证记录

- `3151/actuator/health`：`UP`
- `9628/api/v1/posts/{postId}`：恢复 200
- 抽检 6 篇重写回灌内容：
  - 无 `???`
  - 正文 Markdown 结构正常

## 后续动作

- 将 frontend Nginx 动态上游配置纳入固定发布包，避免再次出现容器 IP 漂移导致的 502
- 在上传前增加自动体检：检测乱码、标题分行、内联标记规范
- 补充“线上抽检脚本”统一验证帖子、评论、PDF 代理三条主链路
